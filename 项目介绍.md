# 行记 · Trip Ledger —— AI 旅行规划与本地发现平台

> 一款融合「AI 行程规划」「附近推荐」「全国地图探索」与「商家推广」的旅行类 Web 应用。
> 核心理念：让每次出发都有迹可循——用算法与地图帮你找到值得去的地方，用 AI 帮你把行程写成可执行的计划。

---

## 一、项目介绍

「行记 · Trip Ledger」面向自由行用户与本地生活商家，提供三类核心能力：

1. **AI 行程规划**：用户输入目的地、天数、偏好与预算，由大模型实时生成结构化行程，过程以流式进度反馈，结果支持 Markdown 导出与局部编辑。
2. **附近推荐与地图探索**：基于高德地图 POI 数据，按距离、评分、热度、标签匹配综合打分，为用户推荐附近目的地；同时提供全国热门景点地图视图，支持搜索定位与景点介绍弹窗。
3. **企业商家推广**：商家可注册企业账号、录入景点/产品、设置推广出价，推荐引擎会把付费产品加权融入推荐结果，并提供数据看板（曝光、点击、趋势）。

项目采用**单页应用 + FastAPI 后端**的轻量架构，数据存储使用 SQLite，缓存层支持 Redis / fakeredis / 进程内三级降级，全程不依赖重型中间件，可在单机快速部署运行。

---

## 二、技术栈

### 后端

| 类别 | 选型 | 说明 |
|------|------|------|
| Web 框架 | FastAPI 0.140 | 异步、自动 OpenAPI 文档、Pydantic 校验 |
| ASGI 服务器 | uvicorn 0.51 | 生产级异步服务器 |
| 数据存储 | SQLite3 | 零依赖本地数据库，`trip_planner.db` |
| 缓存 | Redis / fakeredis / 进程内 LRU | `CacheService` 三级降级 |
| HTTP 客户端 | httpx 0.28 | 异步调用高德地图 API |
| SSE 流式 | sse-starlette 3.4 | 行程规划进度流式推送 |
| LLM 编排 | LangChain 1.3 + langchain-deepseek / langchain-openai | 行程规划 Agent |
| 认证 | 自实现 JWT + pbkdf2_hmac | 无第三方鉴权依赖 |
| 地图数据 | 高德地图 Web Service API | POI 搜索、详情、逆地理编码、IP 定位 |

### 前端

| 类别 | 选型 | 说明 |
|------|------|------|
| 架构 | 单文件 SPA（`index.html`） | 原生 HTML/CSS/JS，无构建步骤 |
| 样式 | Tailwind CSS（CDN） | 原子化 CSS + 自定义品牌变量 |
| 地图 | 高德地图 JS API 2.0 | MarkerCluster 聚合、自定义 InfoWindow |
| 通信 | Fetch API + SSE | REST 请求与流式进度接收 |

---

## 三、功能模块与实现流程

### 3.1 AI 行程规划

**入口**：「规划行程」Tab

**流程**：
1. 用户填写目的地、天数、偏好标签、预算、出发日期（表单数据自动存 `localStorage` 草稿，刷新可恢复）。
2. 目的地输入框接入 `/api/v1/city/suggest` 城市联想，250ms 防抖。
3. 提交后调用 `POST /api/v1/trip/plan`，后端通过 LangChain Agent（`trip_planner_agent.plan_trip_stream`）异步生成行程。
4. 后端以 **SSE** 流式返回 `step` / `progress` / `result` / `error` 事件，前端实时更新步骤进度条。
5. 支持 **AbortController 取消请求**、失败重试（复用上次 payload）。
6. 生成结果支持 **contenteditable 局部编辑**、**一键导出 Markdown**。

### 3.2 发现推荐

**入口**：「发现推荐」Tab

**流程**：
1. **定位**：优先浏览器精确位置（WGS-84），经 `wgs84_to_gcj02` 转换后调用高德 `around` 周边搜索；无精确位置时用 IP 定位（`/api/v1/location/me`）或用户手动选择城市。
2. **逆地理编码**：坐标转「省·市·区」文字展示（`/api/v1/location/regeo`），结果按坐标网格做 7 天 LRU 缓存；定位精度 > 3000m 时显示 `±N km` 精度警告。
3. **推荐打分**（`recommend._score_poi`）：
   - 权重：评分 0.40 + 热度 0.20 + 标签匹配 0.20 + 距离 0.20
   - 「仅看附近」严格模式：评分权重降至 0.35、距离升至 0.25
   - 「值得绕路」宽松模式：评分 ≥ 4.5 的 POI 可突破半径 20% 进入结果
4. **半径自动扩展**：附近结果少于 5 条时自动扩大半径档位（1→3→5→10→20→50 km）。
5. **前端交互**：半径切换、排序（智能/距离优先/评分优先，客户端重排）、仅看附近开关、距离徽章颜色分级（<1km 深青 / 1–5km 黑 / >5km 灰）、推荐理由文案、点击卡片跳转到地图视图。

### 3.3 地图探索

**入口**：「地图探索」Tab

**流程**：
1. 加载高德 JS API，无 JS Key 时整体降级为纯列表。
2. 调用 `/api/v1/hotspots` 获取全国 200 个热门 POI（16 个种子城市 × 5 个类别并发搜索，按 `hot_score` 排序，1 小时缓存）。
3. 使用 `AMap.MarkerCluster` 渲染聚合标记（zoom 3–11 聚合，≥12 散开）。
4. 点击标记打开 **自定义 InfoWindow**（`isCustom: true`），含图片轮播、评分、分类、地址、「加入行程草稿」按钮。
5. **搜索栏**：输入关键词调用 `/api/v1/city/suggest`，点击/回车后：
   - 用 POI 自带坐标直接飞行定位（zoom 15，无动画跳转 + 二次校准）；
   - 调用 `/api/v1/poi/detail` 拉取该 POI 详情，自动弹出介绍窗（图片、评分、人均、地址、电话、营业时间）；
   - 子 POI（如「广州塔(西登塔入口)」）在 place/detail 查不到时，用联想的名称/区域/坐标兜底展示基础介绍；
   - 联想结果按「名称精确匹配搜索词」排序，确保主景点排在首位。
6. 定位后补充该区域 POI 并重建聚合，重建后重新钉死地图视野避免动画中断。

### 3.4 账号与企业商家

**入口**：右上角登录/注册；企业 Tab（merchant 角色可见）

**流程**：
1. **认证**：`pbkdf2_hmac` 密码哈希 + JWT token，`require_auth` / `require_merchant` 鉴权装饰器。
2. **企业资料**：注册 merchant 时自动创建 `enterprises` 记录，商家可编辑名称、地址、联系人、执照号。
3. **产品管理**：商家录入景点/产品（名称、坐标、分类、标签、图片、描述、评分），支持上下架。
4. **推广设置**：设置日预算、千次曝光出价（bid）、账户余额充值。
5. **数据看板**：总曝光/点击/收藏、7 日趋势图、单品统计。
6. **推荐加权**：推荐引擎把企业产品合并进打分结果，推广出价按 `min(bid/200 × 0.10, 0.10)` 加分，被推荐时异步更新 `product_stats`。

---

## 四、核心技术亮点

1. **三级缓存降级**：`CacheService` 优先真实 Redis，失败自动降级 fakeredis，再降级进程内 LRU，保证任意环境都能运行；缓存熔断机制（连续失败 5 次返回上次成功数据）。
2. **双坐标系统一致性**：WGS-84（浏览器/GPS）与 GCJ-02（高德）严格区分，距离计算前必须转换，杜绝系统性偏差。
3. **可解释推荐**：每条推荐附带可读理由（如「评分 4.6，距你 1.8km，匹配 KTV」），而非黑盒分数。
4. **流式 + 可取消的 AI 规划**：SSE 实时进度 + AbortController，用户可随时中断，避免无效消耗。
5. **统一异常契约**：后端全局 `exception_handler(Exception)` 保证任何 500 都返回结构化 JSON，前端 `fetch` 先读 `text()` 再 `JSON.parse`，避免「Unexpected token」掩盖真实错误。
6. **地图交互细节**：自定义 InfoWindow 事件绑定在 DOM 元素上（非 onclick 属性）确保按钮可靠；搜索下拉用 document 捕获阶段 mousedown + 坐标命中检测，避免动态重排导致点击落空；定位后二次校准视野防止聚合重建打断动画。

---

## 五、缺点与不足

1. **单文件前端**：所有 JS/CSS/HTML 集中在 `index.html`，随功能增长可维护性下降，缺少模块化与构建流程。
2. **SQLite 单机限制**：不支持多进程并发写入，企业数据增长后需迁移 PostgreSQL。
3. **推荐算法偏规则**：纯评分+热度+距离+标签加权，缺少用户行为画像与协同过滤，个性化能力有限。
4. **AI 行程质量依赖模型**：LangChain Agent 的行程准确性与结构一致性受 LLM 输出波动影响，缺少行程合理性校验（如景点间交通时间）。
5. **企业推广计费未闭环**：有出价加权与曝光统计，但缺少实时扣费、预算耗尽自动停止、点击计费等完整结算逻辑。
6. **地图无 Key 降级体验弱**：未配置高德 JS Key 时地图功能完全隐藏，仅保留列表，缺少渐进式降级方案。
7. **测试覆盖不完整**：单元测试覆盖推荐/认证/缓存/模型，但缺少前端 E2E 与接口集成测试。
8. **无部署脚本**：缺少 Dockerfile / systemd 服务文件，部署依赖手动操作。

---

## 六、未来规划

### 近期（1–2 个月）

- **前端工程化**：拆分模块（Vite + TypeScript），引入组件化与构建流程。
- **行程校验增强**：引入景点间交通时长估算，校验行程可行性，标注"行程过满/过松"。
- **推广计费闭环**：实现实时余额扣减、预算耗尽自动停推、点击/曝光计费与对账报表。

### 中期（3–6 个月）

- **个性化推荐**：引入用户行为日志（点击、收藏、加入行程），做协同过滤或向量召回，替代纯规则打分。
- **数据库迁移**：SQLite → PostgreSQL，支持多副本部署与更高并发。
- **多端适配**：移动端 PWA 或小程序，利用已有 REST API。
- **内容生态**：用户可发布游记/评价，UGC 内容反哺 POI 介绍（弥补高德无文字简介的短板）。

### 远期（6 个月以上）

- **社交与行程分享**：行程公开分享、多人协作编辑行程。
- **商家 SaaS 化**：多商户入驻、分账、营销工具（优惠券、套餐）。
- **离线能力**：PWA 离线缓存已加载的 POI 与行程，弱网环境可用。

---

## 七、项目结构

```
测试/
├── trip_planner/
│   ├── main.py                # FastAPI 入口，路由注册，全局异常处理
│   ├── recommend.py           # 推荐引擎：打分、热点、企业加权、坐标转换
│   ├── location.py            # IP 定位、逆地理编码（含 LRU 缓存）
│   ├── auth.py                # JWT 认证、密码哈希、鉴权装饰器
│   ├── models.py              # SQLite 表结构与 CRUD
│   ├── cache.py               # Redis/fakeredis/内存三级缓存 + 熔断
│   ├── schemas.py             # Pydantic 请求/响应模型
│   ├── trip_planner_agent.py  # LangChain 行程规划 Agent
│   ├── prompts.py             # 行程规划 Prompt 模板
│   ├── index.html             # 前端单页应用
│   └── dashboard.html         # 企业数据看板页
├── tests/                     # 单元测试（unittest）
├── env_utils.py               # 环境变量与高德 Key 加载
├── requirements.txt
└── 项目介绍.md
```

---

## 八、快速启动

```bash
# 1. 创建虚拟环境并安装依赖
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt

# 2. 配置环境变量（.env）
# AMAP_API_KEY=你的高德Web服务Key
# AMAP_JS_KEY=你的高德JS API Key
# DEEPSEEK_API_KEY=你的DeepSeek Key（行程规划用）

# 3. 启动服务
python -m uvicorn trip_planner.main:app --host 127.0.0.1 --port 8000

# 4. 访问
# http://127.0.0.1:8000
# 演示账号：demo2026 / 123456（企业角色）
```
